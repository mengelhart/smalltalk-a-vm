"String — byte-indexable sequence of characters.
 size is inherited from ArrayedCollection (prim 53).
 at:/at:put: require byte-aware primitives — deferred to Phase 1 closure/byte support.
 Concatenation, reversed, etc. also deferred (need byte at:)."

!String methodsFor: 'printing'!
printString
    ^ self
! !
